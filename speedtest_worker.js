// OurSpeed loads the official LibreSpeed worker at runtime.
// This keeps the frontend static while using LibreSpeed's real measurement engine.
importScripts("https://cdn.jsdelivr.net/gh/librespeed/speedtest@master/speedtest_worker.js");
